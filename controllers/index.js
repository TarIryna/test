const displayName = require('./displayName')
const addUser = require('./addUser')
const findByMail = require('./findByMail')
const fetchData = require('./fetch')

module.exports = {
  displayName,
  addUser,
  findByMail,
  fetchData
}
